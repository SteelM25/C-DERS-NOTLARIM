//#include <iostream>
//using namespace std;

int main(){

    //? Karşılaştırma Operatörleri

    // == eşit
    //  != eşit değil
    //*  > büyük
    //?  < küçük
    //todo:  >= büyük veya eşit
    //!  <= küçük veya eşit

    //--------------

    //? Mantıksal Operatörler

    //!     && and: Birden çok durumdan hepsinin de sağlanması gerekir.
    //todo: || or: Birden çok durumdan en az birinin sağlanması gerekir, sağlanırsa true döner.
    //*     ! not: eşitliğin tam tersine dönmesini sağlar.

    //!         (10<3) && (4<7) = true --- (4>8) && (9<11) = false
    //todo:     (5<=4) || (9>3) = true --- (3<1) || (9<2) = false
    //*         !(3<9) = false (diyorki 3, 9'dan küçük değildir.) --- !(5<2) = true (5, 2'den küçük değil diyor.)


}